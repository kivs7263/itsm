worker_processes auto;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" rt=$request_time';
    access_log /var/log/nginx/access.log main;

    # ── Rate limit zones ─────────────────────────────────────────
    limit_req_zone $binary_remote_addr zone=api_limit:10m     rate=60r/m;
    limit_req_zone $binary_remote_addr zone=auth_limit:10m    rate=10r/m;
    limit_req_zone $binary_remote_addr zone=portal_limit:10m  rate=30r/m;
    limit_req_status 429;

    # ── Upstream (deferred DNS resolution) ───────────────────────
    resolver 127.0.0.11 valid=10s;

    server {
        listen 80;
        server_name _;

        client_max_body_size 50m;

        # ── Health check ─────────────────────────────────────────
        location = /health {
            access_log off;
            return 200 "ok\n";
        }

        # ── 내부 전용 경로 외부 차단 (ADR-048 WF-2) ─────────────
        # /api/internal/* 는 서비스 간 내부 통신 전용 — 외부 인터넷 접근 불가.
        location /api/internal/ {
            deny all;
            return 403;
        }

        # /api/me/products = Next.js 프론트 프록시 라우트(app/api/me/products → SSO 구독제품).
        # 백엔드엔 없음 → exact-match로 프론트 우선 라우팅(안 하면 /api/가 백엔드로 보내 404).
        location = /api/me/products {
            set $frontend http://itsm_frontend:3000;
            proxy_pass $frontend;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ── Backend API ──────────────────────────────────────────
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;

            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 60s;
        }

        # ── SSO 콜백 — rate limit 제외 (KC가 brute force 차단 담당) ──
        location /api/auth/sso {
            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ── Auth 쓰기 엔드포인트 (brute-force 방지, 엄격 rate limit) ──
        # 분리 근거: /api/auth/me·refresh·logout은 페이지 진입·세션 유지용
        # 읽기/갱신 경로라 위 /api/ 일반 존(60r/m·burst 20)으로 낙하해야 함.
        # 구 location /api/auth/ 는 이들까지 묶어 10r/m·burst 5를 소진시켜
        # 정상 탐색 5페이지 만에 429→강제 로그아웃을 유발 (2026-07-09 수정).
        location = /api/auth/login {
            limit_req zone=auth_limit burst=5 nodelay;

            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /api/auth/register {
            limit_req zone=auth_limit burst=5 nodelay;

            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /api/auth/signup {
            limit_req zone=auth_limit burst=5 nodelay;

            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ── SSE (Server-Sent Events) ─────────────────────────────
        location /api/stream/ {
            set $backend http://itsm_backend:8000;
            proxy_pass $backend;

            proxy_set_header  Host              $host;
            proxy_set_header  X-Real-IP         $remote_addr;
            proxy_set_header  X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header  Connection        "";
            proxy_http_version 1.1;
            proxy_buffering   off;
            proxy_cache       off;
            proxy_read_timeout 3600s;
        }

        # ── 고객 셀프서비스 포털 (별도 rate limit) ───────────────
        location /portal/ {
            limit_req zone=portal_limit burst=10 nodelay;

            set $frontend http://itsm_frontend:3000;
            proxy_pass $frontend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ── Next.js 정적 자산 ────────────────────────────────────
        location /_next/ {
            set $frontend http://itsm_frontend:3000;
            proxy_pass $frontend;

            proxy_cache_valid 200 1d;
            add_header Cache-Control "public, max-age=86400, immutable";
        }

        # ── Frontend (catch-all) ─────────────────────────────────
        location / {
            set $frontend http://itsm_frontend:3000;
            proxy_pass $frontend;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Upgrade           $http_upgrade;
            proxy_set_header Connection        "upgrade";
        }
    }
}
